import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  scrollableContent: {
    width: '100%',
    alignItems: 'center', 
    alignSelf: 'center',
    paddingBottom: 400
  },
  scrollableList: {
    padding: 16
  },
});