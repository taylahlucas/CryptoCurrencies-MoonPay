import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  scrollableContent: {
    height: '100%',
    width: '100%',
    alignItems: 'center', 
    alignSelf: 'center'
  },
  scrollableList: {
    padding: 16
  },
});