import { StyleSheet } from 'react-native';

export const textStyles = StyleSheet.create({
  standardText: {
    fontSize: 18,
    fontWeight: '300'
  },
  boldText: {
    fontSize: 18,
    fontWeight: '500'
  }
});